build/
build-*/
.cache/
.docker-test/
compile_commands.json

# benchmark outputs (scripts live in bench/scripts/)
bench/results/*
!bench/results/.gitkeep

# editors / os
.vscode/
.idea/
*.swp
.DS_Store

# internal working documents (private, never committed)
/internal-docs/

# local models & artifacts (never commit weights)
/models/
models/api_keys.txt
*.gguf
*.bin
__pycache__/

# gateway / API secrets (local only)
.env
gateway/api_keys.local.txt
gateway/gateway.json
gateway/.env
gateway/.env.local
